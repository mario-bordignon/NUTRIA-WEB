/**
 * Lógica de interatividade do front-end.
 */

document.addEventListener('DOMContentLoaded', () => {
    // Inicialização da estrutura base.
    // Lógicas futuras de interatividade (como modais ou requisições) devem ser inseridas aqui.
});